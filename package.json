{
  "name": "pastoral-app",
  "version": "1.0.0",
  "description": "App de Seguimiento Pastoral de Miembros de Iglesias",
  "scripts": {
    "dev:frontend": "cd frontend && npm install && npm run dev",
    "dev:backend": "cd backend && npm install && npm run dev"
  }
}
